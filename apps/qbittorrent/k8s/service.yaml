apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: qbittorrent
spec:
  selector:
    app: qbittorrent
  type: LoadBalancer
  ports:
    - name: webui
      port: 8090
      targetPort: 8090
      protocol: TCP
